[tool.poetry]
name = "shuttu-backend"
version = "0.1.0"
description = "Backend for Shuttu Baby Dresses Ecommerce"
authors = ["Antigravity"]

[tool.poetry.dependencies]
python = "^3.11"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
